{
  "name": "@lukaskj/tofu-age-external-encryption",
  "version": "1.0.0",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "prepare": "husky",
    "init:husky": "bunx husky init",
    "build": "bun run scripts/build.ts",
    "declaration": "bunx tsc -p tsconfig.build.json --outDir dist --declaration --emitDeclarationOnly",
    "build:all": "bun run declaration && bun run scripts/build.ts",
    "dev": "bun run src/index.ts",
    "lint": "bunx tsc -p . --noEmit && biome lint --write",
    "format": "biome format --write",
    "test:cov": "bun run test --coverage",
    "test": "bun test --env-file .env.test",
    "prepack": "rm -rf dist && bun run build"
  },
  "dependencies": {
    "age-encryption": "^0.2.4",
    "neverthrow": "^8.2.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.1.4",
    "@faker-js/faker": "^9.9.0",
    "@types/bun": "latest",
    "@types/node": "^24.2.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.1.5",
    "typescript": "^5.9.2"
  },
  "lint-staged": {
    "*.{ts,tsx}": [
      "bun format",
      "bun lint"
    ]
  },
  "main": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": {
        "default": "./dist/index.mjs",
        "types": "./dist/index.d.ts"
      },
      "require": {
        "default": "./dist/index.cjs",
        "types": "./dist/index.d.ts"
      }
    }
  },
  "bin": {
    "tofu-age-encryption": "dist/index.mjs",
    "taee": "dist/index.mjs"
  },
  "engines": {
    "node": ">=23",
    "bun": ">= 1.2.20"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lukaskj/tofu-age-external-encryption.git"
  },
  "bugs": {
    "url": "https://github.com/lukaskj/tofu-age-external-encryption/issues"
  },
  "homepage": "https://github.com/lukaskj/tofu-age-external-encryption#readme",
  "files": [
    "dist/**/*",
    "!**/test/**",
    "!node_modules"
  ]
}
